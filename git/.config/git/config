[alias]
	c = clone --recursive
	co = checkout
	rh = reset --hard
	rs = reset --soft
	rm = reset --mixed
	cfg = vim /.git/config
	psh = push
	pll = pull
	pshf = push -f
	pllf = pull -f
	root = rev-parse --show-toplevel
	bump-submodules = submodule update --remote
	d = diff
	ds = diff --staged
	dt = difftool
	dts = difftool --staged
[user]
	email = miguelsrosa97@gmail.com
	name = msdlr
[difftool]
	prompt = false
[fetch]
	prune = true